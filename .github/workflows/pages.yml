name: Deploy API map to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      # dist/map.html is generated and committed, so it can fall behind
      # apis.json. Refuse to publish a stale offline copy rather than hand one
      # to whoever downloads it. Regenerate with ./tools/bundle.py.
      - name: Check the offline bundle is in sync
        run: ./tools/bundle.py --check

      # The map is static: apis.json is hand-maintained, so there is nothing
      # to build and no nightly job. Publish the repo as-is.
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - id: deployment
        uses: actions/deploy-pages@v4
